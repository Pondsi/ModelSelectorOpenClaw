# ModelSelectorOpenClaw — OpenClaw 控制台模型选择器双列增强

> 把 OpenClaw Control UI（网页控制台）聊天输入框的模型选择器，从官方的「按供应商分组的
> 长条扁平列表」升级为「**左列供应商导航 + 右列仅显示该供应商模型**」的双列 master-detail
> 布局，并隐藏菜单内与选择无关的「此聊天使用的账户」账号行。
> 纯浏览器端运行时增强：**不修改 OpenClaw 任何打包代码**，只在 `index.html` 注入一行
> `<script>` 标签并新增一个独立 JS 文件，随时可一键卸载。

## 这是什么？

一个标准 `SKILL.md` 格式的 AI Agent Skill，可运行在任何支持 Agent Skills 的宿主上——
**原生支持 DeepSeek Harness（dsh）**（放入 `~/.agents/skills/` 即被自动发现），同样支持
OpenClaw、Claude Code、Cursor、Dify、Ollama 及自研 Agent。安装后由 Agent 执行一条
PowerShell 命令完成注入，然后刷新一次浏览器页面即可看到新布局。

它改变的界面：聊天输入框上方点开「模型选择器」后——

```
┌─────────────┬────────────────────────────────┐
│ 全部         │  [ 搜索 ]                       │
│ ▸ DeepSeek  │  deepseek-v4-flash              │
│ ▸ Qwen      │  deepseek-v4-pro                │
│ ▸ GLM       │  deepseek-reasoner              │
│ ▸ Mistral   │  ……仅该供应商的模型              │
└─────────────┴────────────────────────────────┘
```

- 左列：供应商芯片导航，打开时自动高亮当前模型所属供应商
- 右列：只显示所选供应商的模型，独立滚动，互不牵连
- 官方搜索框过滤时自动退回原生扁平列表，清空后自动恢复双列

## 它解决什么问题？

1. **模型多、找得慢**（核心卖点）：配置了多家供应商几十上百个模型时，官方扁平列表要
   滚动很久、目标模型被淹没在其他供应商分组里；双列布局先点供应商再选模型，两步直达。
2. **滚动耦合**：长列表里滚轮体验差；本增强让两列各自独立滚动、互不干扰
   （结构性 CSS Grid 方案，不是补丁式定位）。
3. **「此聊天使用的账户」干扰**：官方会在模型菜单里显示当前凭据账号（如
   `deepseek:default`），它与选择供应商/模型无关，容易让人误以为选错了模型；
   本增强将其隐藏（该功能仍可在官方界面其他位置使用，只是不在菜单里打扰）。
4. **主题割裂**：增强完全使用官方设计 token（`--bg-hover`、`--accent`、`--border`），
   深色/浅色/自定义主题自动跟随，不出现「补丁感」。

## 如何使用？

### 安装（推荐：让 Agent 执行）

对支持技能的 Agent 直接说「给 OpenClaw 控制台装 ModelSelectorOpenClaw」即可；或手动执行：

```powershell
powershell -ExecutionPolicy Bypass -File scripts\pcl-patch.ps1
```

- 脚本**自动检测** OpenClaw 安装目录（npm 全局目录 / 常见前缀 / PATH 里的 openclaw CLI）；
  非标准布局用 `-Dist "<...>\dist\control-ui"` 显式指定。
- 首次运行自动备份原始 `index.html` 为 `index.html.bak-pcl`。
- 看到 `[PCL] 完成` 后，**刷新一次** Control UI 页面即可；从 v1.2.0 起，后续补丁会被
  已打开的页面自动感知并重载，不再需要手动刷新。

### 验证（一行）

浏览器控制台执行：

```js
window.pclModelPickerDiag()
```

返回 `{version, innerWidth, stylePresent, heartbeats, lastEvent, pickers, menus, instances[]}`；
`instances[]` 逐个报告每个选择器实例的 `state` / `twoCol` / `filtering` / `rendered` / `rail` / `chips`。
`state` 为 `two-col` 表示增强已生效，`two-col-filtering` 表示官方搜索过滤中（仍保持双列），
`fallback-narrow` 表示视口低于 340px。页面 `<html>` 上还有 `data-pcl-model-picker="v7"` 标记；
自愈事件用 `window.pclModelPickerLog()` 查看。

### OpenClaw 升级后恢复

npm 升级会覆盖 `dist\control-ui\`（注入消失）。**重跑同一命令**即可，脚本幂等。

Windows 上可以全自动：GitHub 包自带 `platform\pcl-watchdog.ps1`，它比对注入标签与包内脚本
（文件名 + 内容指纹），缺失或过期就自动重跑安装器，注册成计划任务后升级几分钟内自愈；
浏览器端再自动重载页面。该看门狗**仅 GitHub 包含**，不在 ClawHub 制品里。

### 卸载

```powershell
powershell -ExecutionPolicy Bypass -File scripts\pcl-patch.ps1 -Remove
```

移除注入标签与增强脚本，其余一切还原（备份文件保留供核对）。

### 技能安装位置

| 宿主 | 位置 |
|------|------|
| dsh（DeepSeek Harness） | `~/.agents/skills/modelselector-openclaw/` |
| OpenClaw | `<工作区>/skills/` 或 `~/.openclaw/skills/` |
| Claude Code | `~/.claude/skills/modelselector-openclaw/` |
| Cursor / Dify / Ollama / 自研 | 让 Agent 指向本目录，按 SKILL.md 执行 |

## 是否安全？

**是的。** 修改范围被脚本硬性限定，且全部可逆：

| 操作 | 范围控制 |
|------|----------|
| 在 `index.html` 末尾追加/替换**一行** `<script>` 标签 | 首次运行先备份 `.bak-pcl`；幂等；`-Remove` 一键还原 |
| 向 `assets/` 复制 `pcl-model-picker.v7.js` | 带版本号文件名 + URL 内容指纹 |
| 清理旧版本 js | 只删除匹配 `pcl-model-picker.v*.js` 的文件，**保留最近两版** |

- **不修改** OpenClaw 任何打包代码（JS/CSS/SW 全部原样）
- **纯本地**：无网络请求、无遥测、无上传；增强脚本只在你的浏览器里运行
- **作用面**：只改变模型选择器的 DOM/CSS 布局，不触碰会话数据、配置、密钥
- **随时可退**：`-Remove` 卸载，或 OpenClaw 重装自动还原为官方原生界面
- **无自激循环**：所有 DOM 写操作带状态门控（无变化零写入），不会卡 CPU

### 已知约束

- Control UI 的 Service Worker 对 `/assets/` 是 cache-first，且 HTTP 响应是 immutable：
  修改增强脚本内容后请重跑 `pcl-patch.ps1`（它会重算 URL 内容指纹）；即使漏了，v1.2.0 起
  已打开的页面也会自动感知服务端更新并重载。
- 官方若重构选择器 DOM（类名变更），增强需要同步适配。
- 仅视口 < 340px 时退回官方原生布局；官方搜索过滤期间保持双列（v7 起）。
- 同一页面存在多个选择器实例时，只增强当前真正显示着的那个（v7 起）。

## 版本历史要点

### v1.4.0（2026-09-10）

- **修复「页面一直开着，过一段时间增强自己变回原生，刷新才恢复」**：根因是脚本用
  `document.querySelector` 只认文档里**第一个**选择器实例。官方同一套 composer 组件可能
  同时存在多个实例（主聊天 / 侧边会话 / 缓存视图），一旦靠前的那个是隐藏的，增强就全跑到
  隐藏实例上，用户看到的那个退回官方布局；刷新后实例顺序恢复，所以「刷新就好」。
  v7 改为遍历所有实例、**只增强已渲染的那个**。
- **样式表自愈 + 1 秒心跳**：增强样式表被移出文档后会立即重新注入；任何静默失效
  （样式丢失 / 实例被换 / 状态被清）都在 1 秒内自愈，无需刷新。
- **官方搜索过滤不再退回原生布局**：搜索期间保持双列 + 左列，只隐藏没有匹配项的分组；
  点左列芯片会先清空搜索再切换供应商。v6 里可能卡死的 `fallback-filter` 状态已删除。
- 新增 `window.pclModelPickerLog()`（自愈事件环形缓冲）；`pclModelPickerDiag()` 增加
  多实例 / 样式 / 心跳信息；`pcl-patch.ps1` 自动选用版本号最大的脚本。
- 实测（真实 Chromium DOM）：样式被移出 → 心跳内恢复 `display: grid`；隐藏实例靠前 +
  可见实例 → **可见实例保持双列**；可见实例重建 → 双列。

### v1.3.0（2026-09-09）

- **新增自动修复看门狗**（仅 GitHub 包，`platform\pcl-watchdog.ps1`）：OpenClaw 升级会整个
  替换 `dist/control-ui/`，注入标签和增强脚本一起被删。看门狗比对 `index.html` 里的注入标签
  与包内脚本（文件名 + 内容指纹），缺失/过期才重跑安装器；注册计划任务
  `BigLobster-PclModelPickerRepair`（默认每 15 分钟）后，升级几分钟内自动恢复，浏览器端
  再自动重载页面 —— 全程无需手动操作。
- 适配 OpenClaw **2026.9.3**：新版把选择器改为受控 `<details>`（`?open=` 由组件状态驱动，
  触发器在会话未就绪时带 `--disabled`）。增强逻辑不依赖点击事件，只要菜单可见或 `open=true`
  就生效；实测强制打开后 `state: two-col`、10 个供应商芯片正常。
- 看门狗退出码：`0` 正常 / `10` 已修复或需修复 / `1` 出错；日志在
  `%USERPROFILE%\.openclaw\logs\pcl-model-picker-watchdog.log`。

### v1.2.0（2026-09-09）

- **自更新看门狗**：修复「服务端打了补丁，已打开的页面还是旧样式，必须手动刷新」。
  脚本加载后 30 秒、之后每 5 分钟同源拉取一次 index.html（Service Worker 对非
  `/assets/` 路径是 network-first，永远取到最新），解析注入标签的版本号 + 内容指纹，
  发现服务端更新就自动重载页面。
  - 只升不降（服务端版本更旧则忽略，避免来回重载）
  - 用 sessionStorage 记录已尝试的构建，杜绝重载循环
  - 正在输入框打字时不打断，等你停手 60 秒后再重载；后台标签页立即重载
  - 手动触发：控制台 `await window.pclModelPickerCheckUpdate()`

### v1.1.1（2026-09-09）

- 更新署名行为 `Pondsi (+MiMo-v2.5 +deepseek-v4.1-flash-expires-on-0910 +GLM5.3-flash) — automatically committed by OpenClaw`。

### v1.1.0（2026-09-09）

- 项目更名为 **ModelSelectorOpenClaw**（原 PCL Model Picker）。
- **修复「增强突然消失」**：旧版视口宽度阈值 520px 过高——高 DPI 缩放、窄窗口、
  分屏都会让 `innerWidth` 掉到 520 以下，此时脚本会**静默退回原生布局**，用户看到的就是
  「恢复原样」且没有任何提示。现阈值降为 340px，左列宽度改为
  `clamp(84px, 26%, 132px)` 响应式，窄屏不再整体回退。
- **注入 URL 加内容指纹**（`?h=<sha256 前 8 位>`）：Service Worker 对 `/assets/` 是
  cache-first 且 HTTP 缓存 immutable，同名改内容会被旧缓存永久钉死；指纹让内容一变
  缓存键就变。
- **可诊断**：`<html data-pcl-model-picker="v5">`、菜单 `data-pcl-state`
  （`two-col` / `fallback-narrow` / `fallback-filter`）、`window.pclModelPickerDiag()`。
- 菜单查找加兜底：官方若改为顶层浮层/portal 渲染，全文档查找仍能命中。

### v1.0.0（2026-09-09）

- 首个版本：双列 master-detail 布局、两列独立滚动、隐藏账号行、官方 token 主题跟随、
  状态门控防自激、幂等安装/卸载脚本。

## License

MIT 许可证（附**强制署名条款**）：允许使用全部或部分源码（含修改后的变体），但**必须标注
Pondsi 的署名**。详见 [LICENSE](LICENSE)。

---

Pondsi (+MiMo-v2.5 +deepseek-v4.1-flash-expires-on-0910 +GLM5.3-flash) — automatically committed by OpenClaw
